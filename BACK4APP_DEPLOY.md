# نشر مجاني على Back4App Containers (بلا بطاقة ائتمان)

Back4App Web Deployment يوفّر طبقة مجانية **بدون بطاقة ائتمان**، يبني صورة Docker من `Dockerfile` في مستودعك على GitHub (فيُثبّت `ffmpeg` و`yt-dlp` و`zip` تلقائياً)، ويعطيك رابط HTTPS عام.

| البند | المجاني |
| --- | --- |
| المعالج | 0.25 CPU مشترك |
| الذاكرة | 256 MB |
| النقل | 100 GB/شهر |
| المنطقة | الولايات المتحدة |
| البطاقة | ❌ غير مطلوبة |

> ملاحظة: الطبقة المجانية قد تُنيم الحاوية بعد فترة خمول. الحل: منبّه مجاني (UptimeRobot) يزور الرابط كل 5–10 دقائق (الخطوة 5).

## 0) أنشئ/جدّد توكن البوت من BotFather (إلزامي قبل النشر)

> التوكن الحالي في `.env` **قديم وملغى** (طلبات الويب تخفق بـ401). أنشئ توكن جديداً ولا تُظهره لأحد.

1. افتح <https://t.me/BotFather> على تلغرام واضغط **Start**.
2. نفّذ الأمر <code>/newbot</code> واختر اسماً معروضاً ثم يوزرنيم ينتهي بـ`bot` (مثل `MediaSaferBot`).
3. انسخ النص بعد **Use this token to access the HTTP API** (صيغته `123456789:AABB...`).
4. (اختياري) لاستخدام أحدث طريقة تفاعل: نفّذ <code>/newbot</code> ثم من إعداد البوت اختر Inline Keyboard.
5. هذا التوكن الجديد هو قيمة `BOT_TOKEN` — لا تضعه في ملف مرفوع إلى GitHub، بل في متغيرات البيئة عند النشر (الخطوة 3).

## 0.5) ارفع المشروع إلى GitHub من Windows (مرة واحدة)

1. أنشئ مستودعاً جديداً على GitHub (مثل `tg-media-downloader`) — **بدون** إضافة ملفات README أو .gitignore عند الإنشاء.
2. من PowerShell داخل مجلد المشروع:

```powershell
git remote -v
git remote add origin https://github.com/اسمك/tg-media-downloader.git   # إن لم يكن موجوداً
git branch -M main
git push -u origin main
```

3. سيُطلب تسجيل الدخول (GitHub CLI أو PAT). إن واجهت خطأ حول الملفات الكبيرة، لا تضغط `.env` إطلاقاً (تحقق: `git status` يجب ألا يُظهر `.env`، فهو في `.gitignore`).

## 1) أنشئ الحساب

1. افتح <https://www.back4app.com> واضغط **Sign Up**.
2. سجّل بحساب GitHub (أسرع، وبلا بطاقة).

## 2) أنشئ تطبيق Containers

1. من لوحة التحكم: **Build new app** → اختر نوع **Containers** (اسمها أحياناً *Web Deployment*).
2. اربط GitHub وامنح الصلاحية لمستودع `channel2-monitor-bot`.
3. اختر المستودع والفرع **main**.

## 3) الإعدادات المهمة

- **Dockerfile**: سيُكتشف تلقائياً من جذر المستودع (اتركه `Dockerfile`).
- **Port**: `3000`.
- **Health check path**: `/`.
- **Environment Variables** (أضفها كلها):

| المفتاح | القيمة |
| --- | --- |
| `BOT_TOKEN` | توكن البوت من BotFather |
| `OWNER_ID` | معرّفك الرقمي |
| `TELEGRAM_POLLING` | `1` |
| `NODE_ENV` | `production` |
| `PORT` | `3000` |
| `DOWNLOAD_MAX_CONCURRENT` | `2` (التنزيل الثقيل يستهلك ذاكرة؛ لا ترفعه فوق 2 على 256MB) |
| `DOWNLOAD_MAX_WAITING` | `1000` (الطلبات تنتظر بالترتيب ولا تُفقد مع ضغط عالٍ) |
| `SUPABASE_DATABASE_URL` | اختياري لكن موصى به: سلسلة اتصال Supabase/Postgres. يحفظ كل البيانات في قاعدة دائمة (لا تُمسح عند إعادة النشر) ويمنع الرد المزدوج بقفل عالمي على `update_id`. |

> في Dockerfile لدينا `NODE_OPTIONS=--max-old-space-size=192` ليتحمّل حدّ 256MB. اتركه.

## 4) انشر وتحقّق

1. اضغط **Deploy**.
2. راقب **Logs** أثناء البناء ثم التشغيل. ابحث عن:
   ```text
   [Telegram polling] بدأ الاستعلام المحلي عبر getUpdates.
   Server running on http://localhost:3000/
   ```
3. انسخ **App URL** (يشبه `https://<اسم-التطبيق>.b4a.io`).
4. أرسل `/start` إلى البوت على تلغرام؛ يجب أن يرد.

## 5) منع النوم (مهم للعمل 24/7)

1. سجّل في <https://uptimerobot.com> (مجاني بلا بطاقة).
2. **Add New Monitor** → النوع **HTTP(s)** → الرابط = App URL → الفترة **5 أو 10 دقائق**.
3. احفظ.

هذا يبقي الحاوية مستيقظة ويضمن استمرار الاستعلام.

## 6) التحديث لاحقاً

أي `git push` إلى `main` يبدأ إعادة نشر تلقائياً (فعّل **Auto Deploy** إن ظهر الخيار). أو اضغط **Deploy** يدوياً.

للتحديث اليدوي من PowerShell:

```powershell
cd "$env:USERPROFILE\OneDrive\Desktop\ععععععععععععع\telegram-media-downloader-project"
git add .
git commit -m "Update"
git push
```

## بديل احتياطي: ClawCloud Run

- <https://run.claw.cloud> — رصيد `$5` شهرياً مجاناً عند ربط حساب GitHub، بلا بطاقة (يفضّل حساب GitHub عمره 180 يوماً).
- يدعم Docker، ويمكن أن يكفي لتشغيل النسخة 24/7 بمواصفات صغيرة ضمن الرصيد.
- إن أراد المستخدم موارد أكبر (حتى 4 vCPU/8GB) فيُلاحظ أن الرصيد يُستهلك أسرع؛ اختر أصغر نسخة تكفي الاستعلام والتنزيل.

## ملاحظات

- 256MB ذاكرة: البوت يعالج مهمة تنزيل واحدة في كل مرة (طابور)، لذا يكفي للاستخدام الشخصي. تجنّب التنزيلات المتزامنة.
- حدّ إرسال تلغرام 50MB، والمشروع يقيّد الملف بـ45MB.
- الحالة في الذاكرة وتُفقد عند إعادة النشر/النوم؛ أعد إرسال الرابط عندها (زر «أعد المحاولة»).
- ألغِ أي توكن ظهر علناً من BotFather، واستخدم الجديد فقط في متغيرات المنصة.
